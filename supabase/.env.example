# Supabase Auth OAuth Secrets — template
# Copy to supabase/.env and fill in real values. Never commit supabase/.env.
#
# Usage:
#   source supabase/.env && supabase start
#   source supabase/.env && supabase stop && supabase start

SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=

# Apple Sign-In (configure when Apple credentials are set up)
SUPABASE_AUTH_EXTERNAL_APPLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_APPLE_SECRET=

# Spotify API credentials
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
