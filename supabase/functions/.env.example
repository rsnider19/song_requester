# Edge Function secrets — template.
# Copy to supabase/functions/.env and fill in real values. Never commit .env.
#
# SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, and
# SUPABASE_DB_URL are provided by default — do not duplicate them here.

SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
